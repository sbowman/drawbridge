// Package pgxtest is its own Go submodule so that you don't accidentally pull in a
// dependency to the Go PostgreSQL's pgx driver if you're using migrations for another
// type of database server.
package pgxtest
